# 2020.07.14 화요일

- 렌더링 파이프라인

  - 1. 그래픽스 API 초기화 (GPU에게 명령을 전달할 수단을 만들고 사용할 쉐이더를 셋업하는 단계 , GPU디바이스, 커맨드 큐, 렌더링 파이프라인 상태 오브젝트 생성, 데이터로드 )

    2. 드로우콜 생성 ( CPU가 GPU에게 명령을 생성하는 단계)

       드로우콜 : 오브젝트 한 번 그리는데 필요한 명령들의 묶음

    3. 정점 조립 ( 여기부터 GPU가 일한다. )

       정점 버퍼의 요소들을 정점 구조체로 조립 

    4. 버텍스 셰이더 

       정점을 입력받아 다른 형태의 정점으로 변환 

    5. 래스터라이저

    6. 프래그먼트 셰이더

    7. 출력 병함

    8. 최종 화면에 그려진다.

